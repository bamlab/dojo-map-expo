extend type Query {
  allPlaces: [Place]
  searchPlaces(query: String!, aroundLocation: AroundLocationInput): [Place]
}

type Place {
  id: ID!
  name: String!
  address: String!
  latitude: Float
  longitude: Float
  category: PlaceCategory
}

input AroundLocationInput {
  latitude: Float
  longitude: Float
}
